spring:
  application:
    name: UserService

  datasource:
    url: jdbc:mysql://localhost:3309/microservice
    username: root
    password: sayan2005@@S
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8081